import styled from "styled-components";

export const Container = styled.div`
  min-width: min(35px, max(30px, 14vw));
  height: min(35px, max(30px, 14vw));
  object-fit: cover;
  border-radius: 50%;
`;
export const AvatarImg = styled.img`
  width: 100%;
  height: 100%;
  border-radius: inherit;
`;
